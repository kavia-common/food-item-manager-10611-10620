This folder serves the static design assets for the converted screens.
Angular is configured (angular.json) to serve everything under /public at the web root.
Place common.css, screen-specific CSS, and images under /public/assets.
